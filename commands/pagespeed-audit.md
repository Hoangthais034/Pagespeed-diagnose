---
description: Measure and diagnose LCP/performance of a web page using Playwright (mobile emulation, network/CPU throttle, resource blocking, LCP breakdown, CPU profiling)
argument-hint: <url>
---

Invoke the `lcp-audit` skill to perform an LCP/performance audit for URL: `$ARGUMENTS`

If no URL was provided, ask the user for it before starting.
